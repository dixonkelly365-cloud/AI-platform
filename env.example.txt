# --- Optional but recommended: FREE Gemini API key (no card required,
# ever) for unlimited unique story premises. Get one in 2 minutes:
# https://aistudio.google.com/apikey ---
GEMINI_API_KEY=
# Google renames/retires models periodically. If generation ever fails with
# a 404, check https://ai.google.dev/gemini-api/docs/models for the current
# name and set it here — no code change needed.
GEMINI_MODEL=gemini-3.5-flash-lite

# --- Narration voice (character voices are auto-assigned separately, see
# src/tts.js) — this is just the "narrator" role's voice. No Ghanaian-accent
# voice currently exists; Nigerian is the closest real West African option. ---
NARRATOR_VOICE=en-NG-EzinneNeural

# --- YouTube upload (OAuth2 - see README for how to get these) ---
YOUTUBE_CLIENT_ID=xxxxxxxx.apps.googleusercontent.com
YOUTUBE_CLIENT_SECRET=xxxxxxxx
YOUTUBE_REDIRECT_URI=http://localhost:8085/oauth2callback
# This gets filled in automatically the first time you run `npm run auth`
YOUTUBE_REFRESH_TOKEN=

# --- Optional: real AI-generated video via Kling (replaces stock photos
# with actual generated motion). Get one at https://kling.ai/dev/api-key
# (a SEPARATE step from a regular kling.ai account, requires paid API
# credits — see README). Without this, falls back to Pexels photos. ---
KLING_API_KEY=
KLING_MODEL=kling-v2.5-turbo

# --- Real photos for the Ken Burns effect (used when Kling isn't configured) ---
# Free key, instant approval, no cost: https://www.pexels.com/api/
PEXELS_API_KEY=

# --- Video settings ---
VIDEO_TITLE=My AI Generated Story
VIDEO_PROMPT=the invention of the printing press
FPS=30
# Landscape (YouTube long-form): WIDTH=1280 HEIGHT=720
# Vertical (Reels/TikTok/Shorts) - default, matches the short-form script pacing:
WIDTH=1080
HEIGHT=1920
PRIVACY_STATUS=private
BURN_CAPTIONS=true

# --- Instagram (Graph API - requires a Business/Creator account linked to a Facebook Page) ---
IG_ACCESS_TOKEN=
IG_BUSINESS_ACCOUNT_ID=
# Your final_video.mp4 must be reachable at a public URL for Instagram to fetch it.
# Easiest free option: upload it to a public GitHub repo, Dropbox (raw link), or S3 bucket,
# and paste that public URL here after the assemble step.
IG_PUBLIC_VIDEO_URL=
